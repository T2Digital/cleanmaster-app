{
  "name": "clean-master",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -r * dist/ || true",
    "start": "serve dist"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0",
    "firebase": "^10.12.2",
    "@google/genai": "^1.30.0"
  }
}